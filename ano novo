<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Para Minha Noiva ❤️</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: #000;
            color: #fff;
            text-align: center;
            overflow-x: hidden;
        }

        /* Glitter Touch */
        .glitter {
            position: fixed;
            width: 10px;
            height: 10px;
            background: radial-gradient(circle, #ff99dd, #ff33aa, transparent);
            border-radius: 50%;
            pointer-events: none;
            animation: glitterFade 1s linear forwards;
        }

        @keyframes glitterFade {
            0% { opacity: 1; transform: scale(1); }
            100% { opacity: 0; transform: scale(2); }
        }

        .container {
            padding: 40px 20px;
        }

        h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: #ff66cc;
            text-shadow: 0 0 10px #ff99dd, 0 0 20px #ff66cc;
        }

        p {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-top: 20px;
            color: #ffe6f7;
        }

        .heart {
            margin: 40px auto;
            width: 150px;
            height: 150px;
            background: transparent;
            position: relative;
            animation: pulse 1.5s infinite;
            filter: drop-shadow(0 0 15px #ff66cc);
        }

        .heart:before,
        .heart:after {
            content: "";
            position: absolute;
            top: 0;
            width: 75px;
            height: 120px;
            background: #ff66cc;
            border-radius: 75px 75px 0 0;
        }

        .heart:before { left: 75px; transform: rotate(-45deg); transform-origin: 0 100%; }
        .heart:after { left: 0; transform: r

